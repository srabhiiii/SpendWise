<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/trans.css">
    <title>Document</title>
</head>
<body>
    <%- include("../includes/nav2.ejs",{username:username}) %>
    <% if (transactions.length > 0) { %>
        <h1 class="mainline"><%= transactions[0].username %>'s Transactions</h1>
    <% } %>
    
    <% if (transactions.length > 0) { %>
    <div class="filter balance" style="display: flex;">
        <form method="get" action="/<%=transactions[0].username%>/transactions" style="display: flex;">
            <label for="fliterType" style="color: rgb(255, 255, 255);">Choose Category :</label>
            <select id="fliterType" name="filterType" class="filterType filter2 " onchange="this.form.submit()">
                <option value="none" <% if (filterType === 'none') { %> selected <% } %>>ALL</option>
            <option value="income" <% if (filterType === 'income') { %> selected <% } %>>INCOME</option>
            <option value="Expense" <% if (filterType === 'Expense') { %> selected <% } %>>EXPENSE</option>
             </select>

        <label for="filterChoice" style="color: rgb(255, 255, 255);">Choose Date :</label>
        <select id="fliterChoice" name="filterChoice" class="filterChoice" onchange="this.form.submit()">
            <option value="none" <% if (filterChoice === 'none') { %> selected <% } %>>ALL</option>
        <option value="7" <% if (filterChoice === '7') { %> selected <% } %>>Last 1 week</option>
        <option value="30" <% if (filterChoice === '30') { %> selected <% } %>>Last 1 month</option>
        <option value="365" <% if (filterChoice === '365') { %> selected <% } %>>Last 1 year</option>
        <option value="custom" <% if (filterChoice === 'custom') { %> selected <% } %>>custom</option>
        <% if(filterChoice==='custom'){%> 
            <div class="custom-filter ">
                
                    <input type="hidden" name="filtercustom" value="custom" >
                    <input type="date" name="start_date" value="<%= start_date %>">
                    <input type="date" name="end_date" value="<%= end_date %>">
                    <button type="submit">Apply</button>
                
            </div>
        <% } %>
        </select>
        </form>
    </div>
    <div class="viewtrans">

    <table class="table table-striped table-hover table-dark">
        <thead>
            <tr >
                <th style= "background-color:rgb(92, 0, 179);color: rgb(250, 248, 251);">Date</th>
                <th style= "background-color:rgb(92, 0, 179);color: rgb(250, 248, 251);">Type</th>
                <th style= "background-color:rgb(92, 0, 179);color: rgb(250, 248, 251);">Amount</th>
                <th style= "background-color:rgb(92, 0, 179);color: rgb(250, 248, 251);">Category</th>
                <th style= "background-color:rgb(92, 0, 179);color: rgb(250, 248, 251);">View /Delete</th>
            </tr>
        </thead>
        <tbody>
            
            <% for(transaction of transactions) {%>
                <tr href="/<%=transaction.username%>/<%=transaction._id%>">
                    
                    <td><%= transaction.date.toISOString().split('T')[0] %></td></a>
                    <td><%=transaction.type%></td>
                    <td><%= transaction.amount %></td>
                    <td><%= transaction.category %></td>
                    <td class="actions"><form method="GET" action="/<%=transaction.username%>/<%=transaction._id%>"><button type="submit" style="background-color: transparent;"><i class="fa-solid fa-eye" style="color: #ffffff;"></i></i></button></form>/
                        <form method="POST" action="/<%=transaction.username%>/<%=transaction._id%>"><button type="submit" style="background-color: transparent;"><i class="fa-solid fa-trash" style="color: #ffffff;"></i></button></form>
                    </td>
                    
                </tr>

            <%}%>
        </tbody>
        
      </table>
    </div>
    <%} else{%>
        <div class="norec">
        <h1 >OOPS...</h1>
        <br>
        <h1>No Record Found</h1>
        </div>
    <%}%>
    <script src="https://kit.fontawesome.com/f798946187.js" crossorigin="anonymous"></script>
</body>
</html>